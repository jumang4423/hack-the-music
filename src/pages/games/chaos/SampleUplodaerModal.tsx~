import { ChaosGameSettingsType } from "../../../models/chaosGameType";

type Props = {
  gameSettings: ChaosGameSettingsType;
  setGameSettings: (gameSettings: ChaosGameSettingsType) => void;
};

const SampleUplodaerModal = ({ gameSettings, setGameSettings }: Props) => {
  return (
    <div>
      <DialogContent dividers>fuck</DialogContent>
    </div>
  );
};

export default SampleUplodaerModal;
